# Telegram Forwarder Bot — dependencies
# Tested with Python 3.12.0
# The [webhooks] extra installs `tornado`, which is REQUIRED for webhook mode
# (Render). Without it, the bot crashes on startup with:
#   RuntimeError: To use `start_webhook`, PTB must be installed via
#   `pip install "python-telegram-bot[webhooks]"`.
python-telegram-bot[webhooks]==21.6
Telethon>=1.36.0
aiosqlite==0.20.0
python-dotenv==1.0.1
# Optional but recommended: speeds up Telethon's encryption significantly
cryptg==0.4.0
